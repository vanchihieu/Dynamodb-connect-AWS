<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh sửa môn học</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="container-fluid">
        <!-- Header -->
        <div class="row bg-primary text-white py-2 mb-4">
            <div class="col-md-6">
                <h4><i class="bi bi-mortarboard-fill me-2"></i>Hệ thống quản lý môn học</h4>
            </div>
        </div>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-warning text-white">
                        <h5 class="mb-0">Chỉnh sửa môn học</h5>
                    </div>
                    <div class="card-body">
                        <form action="/edit/<%= course.id %>" method="post" enctype="multipart/form-data">
                            <div class="mb-3">
                                <label for="id" class="form-label">STT</label>
                                <input type="text" class="form-control" id="id" name="id" value="<%= course.id %>" readonly>
                            </div>

                            <div class="mb-3">
                                <label for="name" class="form-label">Tên môn học</label>
                                <input type="text" class="form-control" id="name" name="name" value="<%= course.name %>">
                            </div>

                            <div class="mb-3">
                                <label for="type" class="form-label">Loại môn học</label>
                                <select class="form-select" id="type" name="type">
                                    <option value="Bắt buộc" <%= course.type === 'Bắt buộc' ? 'selected' : '' %>>Bắt buộc</option>
                                    <option value="Tự chọn" <%= course.type === 'Tự chọn' ? 'selected' : '' %>>Tự chọn</option>
                                    <option value="Cơ sở" <%= course.type === 'Cơ sở' ? 'selected' : '' %>>Cơ sở</option>
                                    <option value="Chuyên ngành" <%= course.type === 'Chuyên ngành' ? 'selected' : '' %>>Chuyên ngành</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label for="semester" class="form-label">Học kỳ</label>
                                <select class="form-select" id="semester" name="semester">
                                    <option value="1" <%= course.semester === '1' ? 'selected' : '' %>>Học kỳ 1</option>
                                    <option value="2" <%= course.semester === '2' ? 'selected' : '' %>>Học kỳ 2</option>
                                    <option value="3" <%= course.semester === '3' ? 'selected' : '' %>>Học kỳ 3</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label for="department" class="form-label">Khoa</label>
                                <input type="text" class="form-control" id="department" name="department" value="<%= course.department %>">
                            </div>

                            <div class="mb-3">
                                <label for="image" class="form-label">Hình ảnh</label>
                                <input type="file" class="form-control" id="image" name="image" accept="image/*">
                                <% if(course.image) { %>
                                <div class="mt-2">
                                    <p>Hình ảnh hiện tại:</p>
                                    <img src="<%= course.image %>" alt="<%= course.name %>" width="200" class="img-thumbnail">
                                </div>
                                <% } %>
                            </div>

                            <div class="d-flex gap-2">
                                <button type="submit" class="btn btn-warning">Cập nhật</button>
                                <a href="/" class="btn btn-secondary">Hủy</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <footer class="mt-5 text-center text-muted py-3 border-top">
            <p>© 2025 Hệ thống Quản lý Môn học</p>
        </footer>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>